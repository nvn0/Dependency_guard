#include <tunables/global>

profile nodejs-restricted flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>
  #include <abstractions/ssl_certs>

  # Tudo permitido por omissão, bloquear só o perigoso
  /** mrix,
  / r,

  # ── Capabilities ──────────────────────────────────────────────
  capability dac_override,
  capability dac_read_search,
  capability setgid,
  capability setuid,
  capability setfcap,
  capability net_bind_service,

  deny capability sys_admin,
  deny capability sys_module,
  deny capability sys_ptrace,
  deny capability sys_rawio,

  # ── Proc e attr (necessário para namespaces) ───────────────────
  /proc/                        r,
  /proc/**                      r,
  /proc/self/attr/**            rw,
  /proc/thread-self/attr/**     rw,
  /proc/[0-9]*/attr/**          rw,

  # ── Rede ──────────────────────────────────────────────────────
  network inet  stream,
  network inet  dgram,
  network inet6 stream,
  network inet6 dgram,
  network unix  stream,
  network unix  dgram,

  deny network inet  raw,
  deny network inet6 raw,

  # ── Bloqueios de segurança ────────────────────────────────────
  deny /etc/shadow              rwkl,
  deny /etc/passwd              w,
  deny /etc/group               w,
  deny /root/**                 rwkl,
  deny /home/**/.ssh/**         rwkl,
  deny /proc/sysrq-trigger      rwkl,
  deny /proc/kcore              rwkl,
  deny /proc/mem                rwkl,

  deny mount,
  deny umount,
  deny pivot_root,
  deny ptrace,

  # ── Sinais ────────────────────────────────────────────────────
  signal (send,receive) peer=@{profile_name},

  # ── Unix sockets ──────────────────────────────────────────────
  unix (send,receive) type=stream,
  unix (send,receive) type=dgram,
}